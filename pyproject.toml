[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "lookingglass"
version = "0.1.0"
description = "Distributed AI compute by Konomi Systems"
requires-python = ">=3.10"
dependencies = ["numpy>=1.24.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
